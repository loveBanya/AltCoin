# 바이낸스 Futures API 베이스 URL (선택)
# BINANCE_FAPI_BASE=https://your-custom-proxy.workers.dev

# Vercel 배포 시 내부 아시아 프록시(/api/binance) 자동 사용 (기본값)
# 끄려면: BINANCE_USE_INTERNAL_PROXY=false
# BINANCE_USE_INTERNAL_PROXY=false
